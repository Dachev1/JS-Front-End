function sumElements(numbers) {
    const firstElement = numbers[0];
    const secondElement = numbers[numbers.length - 1];

    console.log(firstElement + secondElement);
}

sumElements([20, 30, 40]);